name: Build wheels (no binary tests, only Python bindings tests).

permissions: {}

on:
  workflow_dispatch:

env:
  DISABLE_NATIVE_BINARY_TESTS: true
  BUILD_LINUX: true
  BUILD_MACOS_ARM: true
  BUILD_MACOS_INTEL: true
  BUILD_WINDOWS: true


jobs:
  # Add this tiny config job above all build jobs
  # Super-hacky but should work
  config:
    runs-on: ubuntu-latest
    outputs:
      build_linux: ${{ steps.set.outputs.build_linux }}
      build_windows: ${{ steps.set.outputs.build_windows }}
      build_macos_intel: ${{ steps.set.outputs.build_macos_intel }}
      build_macos_arm: ${{ steps.set.outputs.build_macos_arm }}
    steps:
      - id: set
        run: |
          echo "build_linux=${{ env.BUILD_LINUX }}" >> $GITHUB_OUTPUT
          echo "build_windows=${{ env.BUILD_WINDOWS }}" >> $GITHUB_OUTPUT
          echo "build_macos_intel=${{ env.BUILD_MACOS_INTEL }}" >> $GITHUB_OUTPUT
          echo "build_macos_arm=${{ env.BUILD_MACOS_ARM }}" >> $GITHUB_OUTPUT
  linux:
    name: Build and test wheels for Linux 
    permissions:
      contents: read
    needs: config
    if: ${{ needs.config.outputs.build_linux == 'true' }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: ["ubuntu-24.04", "ubuntu-24.04-arm"]
        python-version: ["3.14", "3.13", "3.12", "3.11", "3.10", "3.9", "3.8"]
    steps:
      # No need unless we test with extras 
      #- name: Install prerequisites
      #  run: |
      #    sudo apt-get update
      #    sudo apt-get install libboost-all-dev libgsl0-dev libeigen3-dev

      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          allow-prereleases: true 

      # Build and run native tests only for Python 3.8
      - name: Build and run native C++ tests
        if: matrix.python-version == '3.8' && env.DISABLE_NATIVE_BINARY_TESTS != 'true'
        run: |
          cd similarity_search
          # No extras at least for now
          #cmake . -DWITH_EXTRAS=1
          cmake .
          make -j 8
          ./release/bunit
          ./release/test_integr
          make clean
          # This is a bit paranoid
          rm -rf ./release


      - name: Build and repair Linux wheels
        if: startsWith(matrix.os, 'ubuntu')
        env:
          PYTHON: ${{ matrix.python-version }}
        # This command also tests wheels
        run: ./build_linux_wheels/deploy.sh

      # Upload artifacts
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.python-version }}-${{ matrix.os}}-${{ runner.arch }}
          path: 'python_bindings/dist/*.whl'

  macos-arm64:
    name: macOS ARM64 (M-series)
    needs: config
    runs-on: ${{ matrix.os }}
    if: ${{ needs.config.outputs.build_macos_arm == 'true' }}
    strategy:
      matrix:
        os: ["macos-15"]
        python-version: ["3.13", "3.12", "3.11", "3.10", "3.9", "3.8"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          allow-prereleases: true 


      - name: Derive python_tag
        run: |
          VER="${{ matrix.python-version }}"
          TAG="cp${VER/./}"
          echo "python_tag=$TAG" >> $GITHUB_ENV

      - name: Verify architecture
        run: |
          clang -dumpmachine
          python3 -c "import platform; print('machine:', platform.machine())"

      - name: Build & run native C++ tests (arm64)
        if: matrix.python-version == '3.8' && env.DISABLE_NATIVE_BINARY_TESTS != 'true'
        run: |
          cd similarity_search
          cmake .
          make -j"$(sysctl -n hw.ncpu)"
          ./release/bunit
          ./release/test_integr
          make clean && rm -rf release

      - uses: pypa/cibuildwheel@v2.20.0
        env:
          CIBW_BUILD: "${{ env.python_tag }}-macosx_*"
          CIBW_ARCHS_MACOS: "arm64"
          CIBW_PRERELEASE_PYTHONS: 1
          CIBW_ENVIRONMENT_MACOS: >
            MACOSX_DEPLOYMENT_TARGET=11.0
            ARCHFLAGS="-arch arm64"
            FORCE_MACOS_ARM=1
          CIBW_TEST_COMMAND: "python -c \"import nmslib; print(nmslib.__version__)\""
        with:
          package-dir: python_bindings

      # Let's test these wheels using tox
      # This assumes we build exactly one wheel
      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install tox
        run: pip install --upgrade pip tox

      - name: Test built wheel with tox
        run: tox -c python_bindings/tox.ini -e py${{ matrix.python-version }} --installpkg wheelhouse/*.whl -q

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.python-version }}-${{ matrix.os}}-${{ runner.arch }}
          path: wheelhouse/*.whl


  macos-x86_64:
    name: macOS Intel
    needs: config
    runs-on: ${{ matrix.os }}
    if: ${{ needs.config.outputs.build_macos_intel == 'true' }}
    strategy:
      matrix:
        os: ["macos-15-intel"]
        python-version: ["3.13", "3.12", "3.11", "3.10", "3.9", "3.8"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          allow-prereleases: true 

      - name: Derive python_tag
        run: |
          VER="${{ matrix.python-version }}"
          TAG="cp${VER/./}"
          echo "python_tag=$TAG" >> $GITHUB_ENV

      - name: Verify architecture
        run: |
          clang -dumpmachine
          python3 -c "import platform; print('machine:', platform.machine())"

      - name: Build & run native C++ tests (x86_64)
        if: matrix.python-version == '3.8' && env.DISABLE_NATIVE_BINARY_TESTS != 'true'
        run: |
          cd similarity_search
          cmake .
          make -j"$(sysctl -n hw.ncpu)"
          ./release/bunit
          ./release/test_integr
          make clean && rm -rf release

      - uses: pypa/cibuildwheel@v2.20.0
        env:
          CIBW_BUILD: "${{ env.python_tag }}-macosx_*"
          CIBW_ARCHS_MACOS: "x86_64"
          CIBW_PRERELEASE_PYTHONS: 1
          CIBW_ENVIRONMENT_MACOS: >
            MACOSX_DEPLOYMENT_TARGET=10.13
            ARCHFLAGS=
          CIBW_TEST_COMMAND: "python -c \"import nmslib; print(nmslib.__version__)\""
        with:
          package-dir: python_bindings

      # Let's test these wheels using tox
      # This assumes we build exactly one wheel
      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install tox
        run: pip install --upgrade pip tox

      - name: Test built wheel with tox
        run: tox -c python_bindings/tox.ini -e py${{ matrix.python-version }} --installpkg wheelhouse/*.whl -q

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.python-version }}-${{ matrix.os}}-${{ runner.arch }}
          path: wheelhouse/*.whl

  windows:
    name: Build and test wheels for Windows
    permissions:
      contents: read
    needs: config
    if: ${{ needs.config.outputs.build_windows == 'true' }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: ["windows-11-arm", "windows-2022"]
        python-version: ["3.13", "3.12", "3.11", "3.10", "3.9", "3.8"]
        # No early Python versions for windows-11-arm
        exclude:
          - os: windows-11-arm
            python-version: "3.10"
          - os: windows-11-arm
            python-version: "3.9"
          - os: windows-11-arm
            python-version: "3.8"
    steps:
      - uses: actions/checkout@v4
      # --- Install Python ---
      # --- Setup Python for x64 using action ---
      - uses: actions/setup-python@v5
        if: runner.arch != 'ARM64'
        with:
          python-version: ${{ matrix.python-version }}
          architecture: 'x64'
          allow-prereleases: true 
      # For ARM extra work is needed
      - name: Install Python for ARM64 ourselves + install tox and uv
        shell: pwsh
        run: |
          Write-Host "Runner architecture: $env:RUNNER_ARCH"
          if ($env:RUNNER_ARCH -eq "ARM64") {
              # Note we are adding a patch number to the path or else it will fail
              # We can't use 3.11.0 though because it was immature and didn't properly register Python
              # Construct full version with patch suffix
              if ("${{ matrix.python-version }}" -eq "3.11") {
                  # Python 3.11.9 fixes ARM registry bugs
                  $pyver = "3.11.9"
              } else {
                  # For all other versions, default to .0 (e.g., 3.12.0, 3.13.0, etc.)
                  $pyver = "${{ matrix.python-version }}.0"
              }
              $mm = $matrixPyVer -replace '\.', ''                 # -> "313"
              $pythonRoot = "C:\Python$mm-arm64"

              # Download Python installer
              $url = "https://www.python.org/ftp/python/$pyver/python-$pyver-arm64.exe"
              Write-Host "Downloading ARM64 Python from $url"
              Invoke-WebRequest -Uri $url -OutFile python-installer.exe

              # Silent per-user install, no launcher, add to PATH, log for diagnostics
              $args = @(
                '/quiet',
                'InstallAllUsers=0',
                'PrependPath=1',
                'Include_test=0',
                'Include_launcher=0',
                'SimpleInstall=1',
                "TargetDir=$pythonRoot",
                "/log=$pwd\py_install.log"
              )
              $proc = Start-Process -FilePath "python-installer.exe" -ArgumentList $args -PassThru -Wait
              if ($proc.ExitCode -ne 0) {
                Get-Content py_install.log -Tail 200 | Write-Host
                throw "Python installer failed with exit code $($proc.ExitCode)"
              }

              Remove-Item python-installer.exe

              $matrixPyVer = "${{ matrix.python-version }}"   # e.g. "3.11", "3.12"
              $majorMinor = $matrixPyVer -replace '\.', ''     # -> "311", "312", etc.
          } else {
              $pythonRoot = & python -c "import os, sys; print(os.path.dirname(sys.executable))"
              Write-Host "Installing Python ${{ matrix.python-version }} via setup-python..."
              echo "::add-mask::${{ matrix.python-version }}"
          }

          if (!(Test-Path "$pythonRoot\python.exe")) {
              throw "Python not found at $pythonRoot"
          }

          $env:PYTHON_ROOT = $pythonRoot
          "PYTHON_ROOT=`"$pythonRoot`"" | Out-File -FilePath $env:GITHUB_ENV -Append
          
          & "$env:PYTHON_ROOT\python.exe" --version
          & "$env:PYTHON_ROOT\python.exe" -c "import platform; print('Python arch:', platform.machine())"
          & "$env:PYTHON_ROOT\python.exe" -m pip install tox uv cibuildwheel

        continue-on-error: false

      # Build and run native tests only for the case of Python 3.8
      - name: Build and run native C++ tests
        if: |
          (
            (runner.arch != 'ARM64' && matrix.python-version == '3.8') ||
            (runner.arch == 'ARM64' && matrix.python-version == '3.13')
          ) && env.DISABLE_NATIVE_BINARY_TESTS != 'true'
        shell: cmd
        run: |
          echo Locating Visual Studio...
          for /f "usebackq tokens=*" %%i in (`vswhere -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set VSPATH=%%i
          if not exist "%VSPATH%" (
            echo ERROR: Could not locate Visual Studio!
            exit /b 1
          )
          echo Using Visual Studio at %VSPATH%

          if "%RUNNER_ARCH%"=="ARM64" (
            call "%VSPATH%\VC\Auxiliary\Build\vcvarsall.bat" arm64 || exit /b 1
            set TARGET_ARCH=arm64
          ) else (
            call "%VSPATH%\VC\Auxiliary\Build\vcvarsall.bat" x64 || exit /b 1
            set TARGET_ARCH=x64
          )

          cd similarity_search || exit /b 1
          cmake -G "NMake Makefiles" . || exit /b 1
          nmake || exit /b 1

          .\bunit.exe || exit /b 1
          .\test_integr.exe || exit /b 1

          del /q *.exe NonMetricSpaceLib.lib || exit /b 1
          nmake clean || exit /b 1

      # --- Build wheels using cibuildwheel ---
      - name: Build wheels with cibuildwheel
        shell: pwsh
        env:
          CIBW_ENVIRONMENT_WINDOWS: >
            DISTUTILS_USE_SDK=1
            MSSdk=1
            CL=/DWINVER=0x0A00 /D_WIN32_WINNT=0x0A00
          CIBW_TEST_COMMAND: "python -c \"import nmslib; print(nmslib.__version__)\""
          CIBW_SKIP: "*-musllinux_*"
        run: |
          $pyver = "${{ matrix.python-version }}" -replace '\.', ''
          $env:CIBW_BUILD = "cp$pyver-*"
          $env:CIBW_PRERELEASE_PYTHONS = 1
          Write-Host "Building only for $env:CIBW_BUILD"
          # Let's get rid of quotes
          $env:PYTHON_ROOT = $env:PYTHON_ROOT.Trim('"')
          Set-Location $env:GITHUB_WORKSPACE


          if ($env:RUNNER_ARCH -eq "ARM64") {
              $env:ARCH = "armv8.0"
              $env:CIBW_ARCHS_WINDOWS = "ARM64"
          } else {
              $env:ARCH = "SSE2"
              $env:CIBW_ARCHS_WINDOWS = "AMD64"
          }

          Set-Location python_bindings
          Write-Host "Building wheels from $PWD"

          Write-Host "Initializing Visual Studio toolchain..."
          $vsPath = & vswhere -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
          if (-not $vsPath) { throw "Visual Studio not found!" }
          $archArg = if ($env:RUNNER_ARCH -eq "ARM64") { "arm64" } else { "x64" }
          $pythonExe = if ($env:RUNNER_ARCH -eq "ARM64") { "$env:PYTHON_ROOT\python.exe" } else { "python" }

          $bat = "$vsPath\VC\Auxiliary\Build\vcvarsall.bat"
          cmd /c "`"$bat`" $archArg && `"$pythonExe`" -m cibuildwheel --platform windows --output-dir dist"
          

          Get-ChildItem dist\*.whl | ForEach-Object {
              & (Join-Path $env:PYTHON_ROOT "python.exe") -m tox -e "py$pyver" --installpkg $_
          }
        continue-on-error: false

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.python-version }}-${{ matrix.os}}-${{ runner.arch }}
          path: 'python_bindings/dist/*.whl'


  source_dist:
    name: Build source distribution
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Build source distribution
        run: |
          cd python_bindings
          python -m pip install --upgrade pip setuptools wheel
          python setup.py sdist

      - name: Upload source artifact
        uses: actions/upload-artifact@v4
        with:
          name: source-dist
          path: python_bindings/dist/*.tar.gz

  aggregate:
    needs:
      - macos-x86_64 
      - macos-arm64
      - linux
      - windows
      - source_dist
    runs-on: ubuntu-24.04
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: combined-artifacts

      - name: Show combined contents (optional)
        run: ls -R combined-artifacts

      - name: Upload combined artifact
        uses: actions/upload-artifact@v4
        with:
          name: all-platform-builds
          path: combined-artifacts/
